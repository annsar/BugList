较严重bug：
、解决材质问题：墙体默认材质丢失，验证墙体贴材质后UV是否正确，区域材质也有问题
、删除墙，墙上的门没有被删除（是否存在bug？）
、解决墙体拖动，门窗的位置不更新的问题：将墙拖动到门上，然后拖动墙，进入3d场景，门的位置与门boolActor的位置不一致
、打开有的以前的方案，没有顶部遮盖
、顶视图下能对墙体进行拖动（线上版本已解决）
、将门、窗拖动到墙上，又没有进行Boolean（已解决）
、区域高度存在问题，原来是2800，现在是3000
、户型绘制界面下，门、窗没有贴在墙上
、拖动墙拾取墙节点，没有共用墙节点（已解决）
、3d场景，选中墙体，墙体属性面板闪烁

性能优化：
、解决编辑器模式下，绘制墙体存在的性能问题
、解决数据加载、回存，无效的数据也被加载、存储的问题

bug：
、线上版本无法加载新保存的墙体数据，是否需要支持数据向上兼容？
、墙体移动逻辑改造？
、3D界面下，选择顶视图，可以拖动墙体？发布分支上已解决
、无法打出Shipping或者BlueprintNativization的包

遗留bug：
编辑器窗口下，墙体删除功能删除区域识别出错

bug是否存在？
、墙体删除是否还存在bug，有时候没有真正删除墙体
、有时候在已知节点上绘制墙体，墙体没有做自动连接
、当前的顶角线和踢脚线是否存在bug？

引擎修改：
WindowsPlatformCompilerSetup.h			C4503

代码规范：
B_Wall改造：
、、A，B点所连接的控制器
、、A，B点所连接的墙体Wall
、、A，B进入非法模式
、、删除DelayUpDate
、、删除LoadingComplete
、、删除SpawnLoadComp
、、删除LoadMode
、、删除TempProcMeshList
、、删除TempTJXProcMeshList
、、替换Mat，AreaMat，TJXMat为WallNode对应的MaterialNode
、、删除TempUVDrawID
、、删除“刷新”和“构建墙体”方法
、、删除GetTriangles
、、删除TextR
、、删除Nor
